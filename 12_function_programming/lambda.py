# lambda arguments: expression
# Lambda arguments: lambda is a keyword in python for defining the anonymonus funciton

'''lambda functions are used when you need a simple function whhich is used only once
in your code or when you use it as a higher function to 
'''
add10 = lambda x: x + 10
print(add10(5))

def add10_func(x):
    return x + 10

mult = lambda x,y: x*y
print(mult(2,7))